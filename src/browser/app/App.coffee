app = require "app"

ElectronKit = require "electron-kit"
{Application} = ElectronKit.Browser

module.exports =
class App extends Application
    initializeModules : ->
        super

    handleEvents : ->
        super

    handleCommands : ->
        super
